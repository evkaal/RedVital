info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/register
  body:
    type: json
    data: |-
      {
          "name": "Admin Test",
          "email": "admin@redvital.com",
          "password": "Admin123!",
          "password_confirmation": "Admin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Logout
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/api/logout
  headers:
    - name: Authorization
      value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
